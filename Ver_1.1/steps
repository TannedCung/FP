#1 Use FaceIndentify.detect_face -> an array of images in shape (i,224,224,3) 
                                    + its (x,y,w,h)
#2 pass images_arr to FaceIndentify.indentify_face -> list_names 
                                                      + list_score

#3 Use the 3 lists-in-order above to pass to draw_label which will draw all in need
        exept the mask status (later on)

#4 Write 1 funcion to return a list of known people and the strangers

#5 later on